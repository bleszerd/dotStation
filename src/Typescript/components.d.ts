interface IProjectCardProps {
    image: string
    title: string
    description: string
    url: string
}

/* Styled */
interface IStyledNavbarContainer {
    opened: boolean
}

interface IStyledMenuContainer {
    active: boolean
}